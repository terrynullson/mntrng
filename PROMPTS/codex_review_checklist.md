P0 (блокирует переход)

Multi-tenant: company_id фильтруется во всех запросах (нет “select * without tenant”)

Нет тяжёлых операций в API (ffmpeg/скачивание сегментов/анализ кадров)

Идемпотентность воркера (повтор job не ломает данные)

Таймауты/ретраи определены

Миграции воспроизводимы, без ручных правок

Логи не содержат секретов/токенов

Коммит содержит только одну логическую задачу

Ответ агента содержит self-check ROLE=<...> CONFIRMED (иначе ответ не принимается).

P1 (не блокирует, но фиксируется)

Нормальные индексы (company_id, stream_id, created_at)

Ошибки типизированы, понятные коды API

Нормальное именование, отсутствие “магии”

Документация обновлена (schema/contract)

Output ReviewAgent (строгий формат)

Summary (1 абзац)

Текстовые файлы в репозитории сохранены в UTF-8 без mojibake/битой кодировки.

P0 issues: список с файлами и строками/функциями

P1 issues: список

Fix prompts: готовые промты для агента-исполнителя
